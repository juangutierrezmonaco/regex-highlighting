export function activateRegexHighlighting() {
  // Colorize texts based on a regex that finds vocals
  console.log('Activate');
}

export function deactivateRegexHighlighting() {
  // Remove the colorize made by the activate command
  console.log('Deactivate');
}

export function usePersonalizedRegex() {
  // Opens a prompt to enter a regex and makes the same action that the activate command
  console.log('usePersonalizedRegex');
}

export function copyTexts() {
  // Selects and copy texts based on the same regex that has uses the activate command
  console.log('copyTexts');
}
